# Typedef Struct Point
This project demonstrates how to use typedef with a struct to define a custom Point type representing a 2D coordinate,and how to access and print its fields.

## What I Learned 
- How to use typedef to simplify struct usage
- How to define a struct representing a 2D point
- How to initialize and access struct fields
- How to format float output with decimal precision
